# Project workflow

After every meaningful change to this project, commit the completed work and push the commit to the `origin` remote. Keep commits focused and verify the relevant checks before pushing when practical.
