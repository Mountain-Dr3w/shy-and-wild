# Photographs

The current JPEGs are replaceable placeholder images, not client work. Add final portfolio images here when they are ready. Use descriptive lowercase filenames with hyphens, then reference them from `/images/<filename>` in `index.html`.
